class Professor {
    constructor(id, name, email, workTime) {
        this.id = id
        this.name = name,
        this.email = email,
        this.workTime = workTime
    }
}

export default Professor